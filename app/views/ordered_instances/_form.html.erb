<%= form_for(@ordered_instance) do |f| %>
    <% if @ordered_instance.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@ordered_instance.errors.count, "error") %> prohibited this single_file_instance from being saved:</h2>

          <ul>
            <% @ordered_instance.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div>
      <br/>
      <%= f.label :label, "Instance label" %>
      <%= f.text_field :label, :placeholder => "Label" %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
