<%= form_for @work, url: wizard_path do |f| %>
    <%= fields_for :work do |builder| %>
        <%= render 'metadata_fields', :f => builder %>
    <% end %>
    <br>
    <div class="action">
      <%= f.submit "Next" %>
    </div>
<% end %>