<h1>Editing book</h1>

<%= form_for @book do |f| %>
    <%= fields_for :book do |builder| %>
        <%= render 'metadata_fields', :f => builder %>
    <% end %>
    <br/>
    <%= f.submit "Update" %>
<% end %>
<% #= render 'form' %>

<%= link_to 'Show', @book %> |
<%= link_to 'Back', books_path %>
