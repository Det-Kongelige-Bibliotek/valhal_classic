<h1>New book</h1>

<%= form_for @book do |f| %>
    <%= fields_for :book do |builder| %>
         <%= render 'metadata_fields', :f => builder %>
    <% end %>
    <br/>
    <%= fields_for :file do |builder| %>
        <%= render 'file_upload', :f => builder %>
    <% end %>
    <br/>
    <%= f.submit "Create" %>
<% end %>
<% #= render 'form' %>

<%= link_to 'Back', books_path %>
