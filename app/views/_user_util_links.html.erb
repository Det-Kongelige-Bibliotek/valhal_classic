<ul class="nav">
  <% if has_user_authentication_provider? %>
      <li class="util-links-login">
        <% if current_user %>
          <%= link_to t('blacklight.header_links.logout'), destroy_user_session_path %> |
          <%#= link_to_unless(current_user.to_s.blank?, current_user, "#") %>
            &nbsp;<font color="white"><%= render :text => current_user %></font>
        <% else %>
            <%= link_to t('blacklight.header_links.login'), new_user_session_path %>
        <% end %>
      </li>
  <% end %>
</ul>