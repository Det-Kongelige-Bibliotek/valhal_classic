<% # header bar for doc items in index view -%>
<div class="documentHeader clearfix">
    <% # main title container for doc partial view -%>
    <h5 class="index_title"><%= t('blacklight.search.documents.counter', :counter => (document_counter + 1 + @response.params[:start].to_i)) %>
      <% #Override the main title container view in Blacklight so that the hyperlink for the Book or Person links to the landing page for it  %>
      <% if document['active_fedora_model_ssi'].eql? 'AuthorityMetadataUnit' %>
        <a href="<%= "/authority_metadata_units/#{document.id.to_s}" %>"><%=document[document_show_link_field] %></a>
      <% else %>
        <a href="<%= "/#{document["active_fedora_model_ssi"].pluralize.to_s.downcase}/#{document.id.to_s}" %>"><%=document[document_show_link_field] %></a>
      <% end %>
    </h5>
</div>