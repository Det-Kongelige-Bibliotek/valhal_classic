<%= form_for Work.new, :url=>batch_edits_path, :method=>:put do |f| %>

  <div class="control-group">
    <%= f.label :workType, "Type:"  %>
    <div class="controls">
      <%= f.text_field :workType %>
    </div>
  </div>
  <div class="form-actions">
    <%= f.submit "Save changes", :class=>'btn btn-primary updates-batches'%>
  </div>

<% end %>
